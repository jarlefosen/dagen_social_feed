{
  "name": "dagen_social_feed",
  "version": "0.0.2",
  "devDependencies": {
  },
  "scripts": {
    "main": "server.js",
    "postinstall": "(cd app; ../node_modules/.bin/bower install; cd ..)",
    "prestart": "npm install",
    "start": "node server.js"
  },
  "dependencies": {
    "bower": "^1.3.12",
    "express": "^4.10.0"
  }
}
