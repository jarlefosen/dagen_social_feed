{
  "name": "dagen_social_feed",
  "version": "0.0.2",
  "devDependencies": {
    "http-server": "^0.7.2"
  },
  "scripts": {
    "postinstall": "cd app; bower install; cd ..",
    "prestart": "npm install",
    "start": "node_modules/.bin/forever node_modules/.bin/nodemon --exitcrash server.js"
  },
  "dependencies": {
    "bower": "^1.3.12",
    "forever": "^0.11.1",
    "nodemon": "^1.2.1",
    "express": "^4.10.0"
  }
}
